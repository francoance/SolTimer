<Window x:Class="SolTimer.CompactTimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:SolTimer"
        mc:Ignorable="d"
        Title="Compact Timer" Height="50" Width="150"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStyle="None" Opacity="0.6"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid Background="#FF333333">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="TimerDisplay" 
                   Text="00:00:00" 
                   FontSize="24" 
                   Foreground="White"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   MouseDown="TimerDisplay_MouseDoubleClick" Width="90"/>

        <Button Grid.Column="1"
                x:Name="PauseButton"
                Width="30"
                Height="30"
                Margin="5"
                Background="Transparent"
                BorderThickness="0"
                Click="PauseButton_Click">
            <iconPacks:PackIconJamIcons x:Name="StatusIcon" Foreground="White" Kind="Play" />
        </Button>
    </Grid>
</Window> 